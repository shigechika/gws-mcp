---
"@googleworkspace/cli": patch
---

ci(release): use friendly asset names (linux-amd64, macos-arm64, etc.) instead of raw Rust target triples. Add asset_name field to build matrix. Update Homebrew formula URLs and FORK.md install commands accordingly.
